// Declare constant

// Can not change the value of ConstantSourceNode(const)

const pi = 3.14;
console.log(pi);

// can do like but can not change the value of ConstantSourceNode(const)
console.log(pi*2);